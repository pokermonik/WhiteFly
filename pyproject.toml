[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "projekt_rekrutacyjny"
version = "0.1.0"
dependencies = [
    "flask",
    "fastapi",
    "uvicorn",
    "python-multipart",
    "jinja2",
    "celery",
    "redis"
]